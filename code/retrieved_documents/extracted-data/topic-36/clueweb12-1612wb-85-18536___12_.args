also, ezsql provide a few methods which simplify queries to the database, and help producing a cleaner code.everyone wants sql to be easy, and indeed this script does seem like an easy way to avoid having to change your sql function calls if you change databases.view full user profile nowadays, most websites and web applications are database driven, which means that you, the developer, have to query the database to get the requested information.let’s take a look at a very useful tool to handle sql queries easily and efficiently on small to medium projects.ezsql does not offer any way to parameterize your queries – or indeed to protect your queries from sql injection at all — least of all in a system-portable fashion.as you can guess, ezsql allows you to work with various databases very easily.in order to insert, delete or most generally, run any kind of query to the database, we have to use the query method.on big projects, the usual good practice is to use a cms or a framework such as symfony or codeigniter to build your site on.this is done easily using your database username, password, name and host.let’s see what you can do with it: in order to insert, delete or most generally, run any kind of query to the database, we have to use the query method.ezsql has a few methods to make sql queries extremely simple.it’s a great tool which was very helpful for me many times!as most of you are familiar with wordpress, you probably know the wpdb class, which allows you to send queries to the database.in order to use ezsql with a mysql database, you have to include ez_sql_mysql.php.when called, the method will display the last query performed and its associated results.